{% extends 'base.html.twig' %}
{% block title %}{{ 'genre.deletetitle'|trans }}{% endblock %}

{% block body %}

    <div class="offset-sm-2 offset-md-0 col-sm-8 col-md-6" >
        <h1 class="pb-4">{{ 'genre.delete.title'|trans }} "{{  genre.name }}" ?</h1>
        <p>{{ 'genre.delete.message'|trans }}</p>
        <div class="mb-5">
            <a href="{{ path('genre_delete',{id: genre.id}) }}" class=" mb-5 btn btn-primary">{{ 'genre.delete.confirm'|trans }}</a>
        </div>
        <div class="mt-5">
            <a href="{{ path('genre_index') }}" class="btn btn-primary mb-3">{{ 'genre.delete.back'|trans }}</a>
        </div>

    </div>

{% endblock %}